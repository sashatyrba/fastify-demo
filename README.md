# Tiny Fastify (+Swagger demo)

```shell
docker-compose up
yarn knex migrate:up
yarn knex seed:run
yarn dev
```

Open `localhost:3000/docs` for Swagger.

Default user is `user@example.com` with password `password`.

Have fun!
